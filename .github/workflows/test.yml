name: Test Project
on: push
jobs:
    tests:
        runs-on: ubuntu-slim
        steps:
            - name: Get code
              uses: actions/checkout@v6
            - name: read content
              run: cat flow.txt 
            - name: run python code
              run: python3 demo.py

